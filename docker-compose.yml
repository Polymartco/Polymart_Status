services:
  status:
    build: .
    ports:
      - "3001:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -fs http://localhost/ || wget -qO- http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
